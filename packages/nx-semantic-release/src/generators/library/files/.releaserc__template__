branches:
<%parsedBranches.map((branch) => { -%>
  - <%= branch %>
<%}) -%>
<%parsedPrereleaseBranches.map((prereleaseBranch) => { -%>
  - name: <%= prereleaseBranch %>
    prerelease: true
<%}) -%>
debug: true
ci: true
dryRun: false
plugins:
  - '@semantic-release/commit-analyzer'
  - '@semantic-release/release-notes-generator'
  - - '@semantic-release/npm'
    - pkgRoot: ./dist/<%= projectRoot %>
      tarballDir: ./dist/<%= projectRoot %>
  - - '@semantic-release/github'
    - assets:
        - path: ./dist/<%= projectRoot %>/*.tgz
          label: <%= name %> (tar.gz)
